{
  "name": "moodlenet3",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "bs": "lerna bootstrap --hoist --nohoist=babel-* --nohoist=*react* --nohoist=@ant-* --nohoist=antd*",
    "exe": "lerna run --parallel --stream"
  }
}
